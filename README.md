##Deployment:

##INSTALLATION


1.Clone the Repository

   ```sh
   git clone <your-repository-url>
   cd <your-repository-directory>

2.Create a virtual environment (optional but recommended):

python -m venv venv
source venv/bin/activate

3.Install the required packages:

pip install streamlit

Save the script and run the app streamlit run program.py
# Task1-Tic Tac Toe with AI

This is a simple Tic Tac Toe game with AI using the minimax algorithm, built with Streamlit.

TASK1-CHATBOT WITH RULE-BASED RESPONSES
